rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest

server:
  port: 8081


spring:
  cloud:
    stream:
      bindings:
        producerOutput:
          destination: ProducerDestination
          content-type: application/json
          group: ProducerDestinationGroup
